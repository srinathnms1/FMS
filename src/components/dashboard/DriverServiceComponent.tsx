import * as React from 'react';
import { GraphComponent } from '../../core/GraphComponent';

const DriverServiceComponent = () => {
    return (
        <div>DriverServiceComponent!</div>
    );
};

export default DriverServiceComponent;